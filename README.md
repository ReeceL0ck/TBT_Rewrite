# TBT_Rewrite
This is a rewrite of the TBT project writtien with Django and uses UV as the package manager

# To Run
1. Clone the repo 
2. cd into directory
3. use uv run manage.py runserver to start local server
